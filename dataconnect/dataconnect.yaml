specVersion: "v1beta"
serviceId: "majorproject"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "majorproject-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
